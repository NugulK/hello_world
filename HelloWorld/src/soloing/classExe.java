package soloing;

public class classExe {
	//클래스의 구성 멤버
		// 필드(filed) - 객체의 데이터가 저장되는곳
		// 생성자(Constructor) - 객체 생성시 초기화 역활 담당
		// 메소드(Method)객체의 동작에 해당하는 실행블록
	
	/*
	 * 클래스: 객체를 만들기 위한 설계도
	 * 객체: 클래스로부터 생성되며 new 클래스()로 생성합니다.
	 * new 연산자 : 객체 생성 연산자이며, 생성자를 호출하고 객체 생성 번지를 리턴합니다.
	 * 클래스 변수 : 클래스로 선언한 변수를 말하며 해당 클래스의 객체번지가 저장됩니다.
	 * 인스턴스 : 객체는 클래스의 인스턴스입니다.
	 * 클래스 멤버: 클래스에 선언되는 멤버는 필드, 생성자, 메소드가 있습니다.
	 */
}
